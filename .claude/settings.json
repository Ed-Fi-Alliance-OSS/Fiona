{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path'); case \"$f\" in */apps/fiona-slack/*) (cd apps/fiona-slack && npx @biomejs/biome format --write \"$f\") 2>/dev/null || true ;; esac",
            "timeout": 30
          }
        ]
      }
    ]
  }
}

{
  "name": "postmortem-capture",
  "private": true,
  "type": "module",
  "description": "PR post-mortem data capture (Phase A). No runtime dependencies.",
  "scripts": {
    "test": "node --test"
  }
}
